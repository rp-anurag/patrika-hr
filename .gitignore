# Dependencies
node_modules/

# Environment variables — NEVER commit secrets
.env

# Uploaded resumes — large binary files, stored on server only
uploads/*
!uploads/.gitkeep

# SSL certificates — generated locally, never commit private keys
certs/

# OS / editor
.DS_Store
Thumbs.db
*.log
